spring:
  cloud:
    config:
      name: config-client-development, config-client-production
      uri: http://localhost:8888
  profiles:
    active: development
  application:
    name: config-client
